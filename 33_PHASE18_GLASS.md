# 33_PHASE18_GLASS — 위계 재정립·리퀴드글래스·접수 시스템 (Opus, 단독→병렬3→단독)

## 절대 원칙
1. 색상은 docs/CI.md HEX만. 새 색 생성 금지. 사용자 원문(공지·성과·법적 문구 원안) 변경 금지.
2. 실측 진단: 현재 텍스트가 전부 #F7F5FC 단일이고 강조 보라 면적 1%. 문제는 보라 과다가 아니라 위계 실종이다. 보라·연보라 포인트를 규칙에 따라 되살리되 CI.md 4.3(강조 보라 ≤10%)은 유지.
3. 네이티브(맥/브라우저 기본) select·date input UI를 전역에서 우리 컴포넌트로 대체한다.
4. 시스템 프롬프트/스킬: .claude/skills/ 의 apple-design, improve-animations, review-animations 스킬을 이 배치 전체에서 활용하라(디자인 결정 시 참조).

---

# STEP 1 — 단독 (공용·시스템, 반드시 먼저)

## X1. 텍스트 위계에 보라 포인트 복원
CI.md 4.4·4.5 안에서 포인트 규칙을 컴포넌트화하라:
- eyebrow/섹션 라벨(PROGRAMS 등): text.ter 유지하되 숫자(01 등)는 purple.mid
- 연도·기수·고유명(운영위 2026, LUCID, 제1대 등): purple.light(#C8B9F2)
- 직책 라벨(위원장·부위원장·회장·부회장·학회장·부학회장): purple.mid(#A286E9)
- 링크·키워드·상태: 기존 링크 토큰
- 본문은 계속 text.sec. 보라 문장 3줄 연속 금지 유지.
공용 Accent 텍스트 유틸(또는 클래스 규약)을 만들어 Y 에이전트들이 쓰게 하라.

## X2. 버튼 위계 재정립 (생화이트 문제)
현재 Primary가 밋밋하다. CI.md 버튼 토큰 위에 질감·위계를 더하라:
- Primary: bg #815FD7 + 상단 화이트 하이라이트 라인(inset) + 은은한 퍼플 글로우 섀도우(0 4px 24px rgba(129,95,215,0.35)) + hover 시 글로우 강화 + pressed 감광
- Secondary: 유리 패널(#211A31 반투명) + hairline 보더 + hover 시 보더 purple
- Ghost: 텍스트 연보라 + hover 유리 배경
- 포커스 링 #A286E9 유지. 전 버튼이 이 3위계 중 하나만 쓰도록 Button 컴포넌트 정리.

## X3. 리퀴드글래스 도입 (liquidGL)
사용자가 client/vendor/liquidgl/ 에 liquidGL.js와 html2canvas.min.js를 배치한다(배치 방법은 채팅 안내 참조). npm liquid-gl이 설치 가능하면 npm 우선.
- 적용 대상은 성능을 위해 핵심 fixed 표면만: (1) 데스크탑 헤더 바 (2) 전시회 피처드 블록의 CTA 패널 (3) 모바일 햄버거 시트. 그 외 카드에는 적용 금지(성능).
- WebGL 실패·모바일 저성능 시 기존 CSS 글래스로 폴백(기능 저하 없이).
- 동시에 CSS GlassCard를 강화: 상단 1px 화이트 하이라이트(10~16%), 내부 은은한 그림자, hover 시 hairline.purple. 전 카드가 유리 위계를 되찾게.

## X4. 선 장식 전역 제거
"01 PROGRAMS" 같은 eyebrow 옆/사이의 짧은 가로선(height≤2px 장식 라인, 실측 4개+) 전역 제거. hr·divider성 짧은 선 전부. 콘텐츠 구분 헤어라인(카드 보더)은 유지하되 장식용 짧은 선만 제거.

## X5. 네이티브 UI 전수 대체 [공용 컴포넌트]
공용 Select(커스텀 드롭다운: 유리 패널·키보드 접근·검색 없는 단순형)와 DatePicker(우리 디자인 캘린더: 월 그리드·연/월 네비·시간 입력)를 만들어라. 그리고 코드베이스 전수 검색으로 <select>, <input type="date">, <input type="datetime-local"> 사용처를 전부 찾아 교체하라(히어로 편집 "위치" 셀렉트 포함, 어드민 전체 포함). 라디오·체크박스도 기본 브라우저 스타일이 노출되는 곳은 커스텀으로. 교체 목록을 출력하라.

## X6. 어드민 사이드바 상태 유지
어드민 좌측 메뉴 클릭 시 화면이 초기화되며 스크롤이 위로 리셋되는 문제 수정: 사이드바는 리마운트 없이 유지, 콘텐츠 영역만 교체, 사이드바 스크롤 위치 보존.

STEP 1 빌드 확인 후 STEP 2 병렬.

---

# STEP 2 — 병렬 (Y1 홈·헤더·푸터·상담 / Y2 전시회·접수 / Y3 어드민·시트·데이터)
소유: 공용 컴포넌트(X 산출물)는 수정 금지, 사용만. Y1=홈·헤더·푸터·상담·법적·운영위 표시. Y2=전시회 공개+접수 공개 플로우. Y3=어드민·접수 관리 시트·성과·동아리.

## AGENT-Y1

### Y1-1. 프로그램 섹션 정렬
우측 상세 패널의 "02" 숫자 삭제. 패널 상단을 좌측 리스트 상단과 같은 가로 높이로 정렬(현재 VIEW MORE가 좌측 끝보다 아래로 벗어남). 우측 패널 전체 높이도 좌측과 정합.

### Y1-2. 모바일 내비 햄버거 복귀
글래스 도크를 폐기하고 상단 헤더 우측에 [KR/EN 버튼(모두)] [설정 아이콘(로그인 관리자만)] [햄버거] 순서로 배치. 햄버거 열면 유리 시트: 최상위 메뉴 목록, 각 항목 우측 > 아이콘, 탭하면 하위가 아코디언으로 펼쳐짐(처음엔 전부 접힘). 하위 탭 시 이동·시트 닫힘. 설정 아이콘은 어드민 진입.

### Y1-3. 히어로 정리
버튼 1·2 모두 텍스트만(버튼2의 → 화살표 아이콘 제거). X2 버튼 위계 적용(1=Primary, 2=Secondary). 히어로 텍스트 위계에 X1 포인트 적용해 밋밋함 해소.

### Y1-4. 운영위 표시 보라 포인트
연도(2026)·기수명(LUCID 등)=purple.light, 직책(위원장·부위원장·회장·부회장·학회장·부학회장·각 부서 라벨)=purple.mid. 멤버 이름은 text.pri 유지.

### Y1-5. 편집 버튼 정리
공개 페이지의 "편집 / 전체 관리" 이중 버튼을 아이콘+「편집」 하나로 통합(전체 관리 문구 삭제, 클릭 시 기존 전체 관리 동작).

### Y1-6. 푸터·법적 페이지
1. 푸터의 개인정보처리방침·이용약관·상담 신청 링크를 새 탭에서 열리게 하고 열릴 때 최상단 스크롤 보장.
2. 개인정보처리방침·이용약관을 상세 재작성: 수집 항목에 전시회 접수(인적사항·이메일·연락처·작품 정보·수정용 비밀번호)와 상담 신청, 쿠키·분석(GA 예정), 보유 기간, 제3자 제공 없음, 파기 절차, 권리, 문의처(학과 이메일) 포함. 대학 학과 사이트 수준의 격식으로.

### Y1-7. 상담 신청 재설계
1. 마진 오버플로우 수정.
2. 레이아웃: 좌측 입력(레이블 축약: 이름·학년·주전공·복수전공·연락처·문의), 필드 폭을 내용에 맞게(3~4글자 레이블에 전체폭 금지, 2열 배치 활용), 문의 내용만 넓게.
3. 우측에 결제창식 동의 패널: 개인정보 수집 안내 요약 + 동의 체크 + [상담 신청하기] 버튼. 동의 체크 전에는 버튼 비활성. 업로드된 CardForm.jsx·PayMethodGrid.jsx의 패턴(자동 포맷·grid-rows 확장·선택 링)을 참고하되 색·토큰은 우리 것.
4. 연락처 자동 하이픈(Y2-4와 동일 유틸 공용).

## AGENT-Y2

### Y2-1. 전시회 페이지 리퀴드글래스 강화
메인(피처드) 전시회 블록을 더 키우고 유리 질감의 정점으로: 포스터 주변 유리 프레임, CTA 패널 liquidGL(X3), 배경에 은은한 퍼플 글로우, 하단 그리드 카드도 GlassCard 강화 적용. 디자인 토큰 범위 안에서.

### Y2-2. 전시회 상세 여백 축소
상세 페이지 상단(브레드크럼~헤드라인~포스터) 수직 여백을 대폭 줄여 첫 화면에서 헤드라인+포스터+핵심 정보가 스크롤 없이 보이게.

### Y2-3. 접수 온보딩
접수 버튼 클릭 시 바로 폼이 아니라 온보딩 화면 먼저: 전시회명, 접수 대상·기간(크게), 절차 안내(작성→제출→수정 방법), 유의사항(작품명 '-' 규칙 등), [접수 시작하기] 버튼. 그 다음 폼.

### Y2-4. 접수 폼 정비
1. 마진 초과(튀어나옴) 수정, Container 준수.
2. 작품 이미지 업로드 필드 삭제.
3. 수정용 비밀번호·비밀번호 확인: 세로 배치(위아래), 4자 이상 검증.
4. 이메일: @ 포함 형식 검증(불통과 시 제출 불가).
5. 연락처: 자동 하이픈 강제. 010 입력 시 자동 -, 4자 후 자동 -, 이후 4자까지만. 그 외 문자는 입력 자체가 안 되게. (010-XXXX-XXXX 고정)
6. 작품명 필드 아래 안내: "작품명에 '-'를 사용하는 경우, 구글 클래스룸 업로드 시 '_'로 사용해 주시기 바랍니다."
7. 접수 시작·접수 마감·수정 마감 날짜를 폼 상단에 크게 강조(유리 패널 + purple.light 날짜 강조, 현재처럼 작고 좌측 구석 금지).
8. 과목 선택: 관리자가 등록한 과목 목록(Y3-1)을 구글 폼처럼 잘 보이는 카드형 라디오로 선택(시스템 셀렉트 금지, PayMethodGrid의 선택 링 패턴 참고).

### Y2-5. 접수 확인·수정 진입 [신설, 중요]
일반 사용자가 본인 접수를 다시 보고 수정하는 입구가 없다. 전시회 페이지(접수 버튼 옆)와 온보딩에 "접수 내역 확인·수정" 진입을 만들고 플로우 구현:
이메일+수정용 비밀번호 입력 → 서버 검증 → 본인 접수 목록(여러 건이면 선택, 1건이면 바로) → 수정 화면(읽기전용: 참가 유형·과목·이메일 / 수정 가능: 인적사항·연락처·작품명·작품 설명·비밀번호) → 저장 → 완료 화면. 수정 마감 검증은 서버에서. 업로드된 기존 앱스 스크립트 폼(이메일 조회→목록 선택→수정→완료)의 UX 플로우를 참고하되 디자인은 우리 시스템.

## AGENT-Y3

### Y3-1. 전시회 설정 확장
어드민 전시회 설정에: (1) 접수 버튼 노출 스위치가 보이게(현재 히어로 편집 안에만 있음) (2) 과목 관리 신설: 1학기 과목·2학기 과목 목록을 추가/수정/삭제(예: UX디자인, 디지털디자인1 등). 접수 폼(Y2-4-8)이 이 목록을 읽는다. 학기 구분 저장.

### Y3-2. 접수 관리 시트 페이지 [신설, 대형]
어드민에서 "접수 현황 열기" 클릭 시 새 탭으로 열리는 전용 관리 페이지(/admin/exhibition-entries/sheet 등, 어드민 권한 가드):
- 구글 시트 스타일: 상단 탭(과목별 필터 + 전체), 컬럼 헤더 고정, 행 스트라이프, 셀 클릭 시 선택·복사(클립보드), 행 전체 복사, 컬럼 정렬, 검색.
- 내려받기: CSV와 엑셀(xlsx) 버튼.
- 실시간: 수동 새로고침 버튼 + 자동 새로고침(15~30초 폴링, 토글로 켜고 끄기), 마지막 갱신 시각 표시.
- 디자인은 구글 시트를 따라하되(밝은 그리드, 명확한 셀 경계) 우리 다크 시스템과 충돌하지 않게 이 페이지만 라이트 시트 스타일 허용.

### Y3-3. 대시보드 콘텐츠 공개/비공개 토글
대시보드 콘텐츠 현황 요약의 각 콘텐츠 유형에 공개/비공개 토글. 비공개면 공개 사이트에서 해당 섹션·메뉴 숨김. 포트폴리오를 공개로 전환하면 학생 활동 하위 메뉴에 "포트폴리오"가 나타나게 연동.

### Y3-4. 학생 성과 정렬·드래그 [재지시, 아직 미반영]
신규 등록이 해당 연도 맨 위로 오게 정렬 수정 + 6점 드래그 핸들로 같은 연도 안 순서 변경(어드민·공개 편집 모두). 원인 진단 포함. 드래그 저장 후 새로고침 유지 검증.

### Y3-5. 동아리 상세·링크
동아리에 상세 페이지 신설(로고·이름·분야·소개·활동·사이트 링크 버튼) + 목록 카드 클릭 진입. site_url 필드 추가, 관리자 편집(인라인·어드민 폼). 링크는 새 탭.

---

# STEP 3 — 단독 통합
1. 충돌 확인, 네이티브 UI 잔존 재검색(0건), 선 장식 잔존 재검색(0건), build.
2. 커밋·푸시·배포. 배포 후: 홈 위계(보라 포인트·버튼 질감), 모바일 햄버거, 전시회 피처드·온보딩·접수폼·수정 진입, 접수 시트, 대시보드 토글, 성과 드래그 육안 확인.
3. PROGRESS.md 기록(liquidGL 적용 위치, 스킬 사용 내역 포함).