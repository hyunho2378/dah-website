# 35_HEADER_HOVER_WHALE — 헤더 호버 시 사라짐 (Whale 브라우저) 진단·수정 (단독)

## 재현된 증상 (사용자 실기기 확인)
네이버 웨일(Whale) 브라우저에서, 헤더 메인 메뉴 5개 중 하나에 호버하면(드롭다운이 열리는 순간) 헤더 전체가 사라지거나 안 보이게 된다. 클릭 문제(34번에서 pointer-events로 수정 완료)와는 별개의 새로운 증상이다.

## 가설 (검증 필요, 확정 아님)
liquidGL은 대상 요소(.dah-liquid-header)를 시각적으로 opacity:0 또는 그에 준하는 방식으로 숨기고, 그 자리에 html2canvas로 뜬 스냅샷을 WebGL 캔버스로 렌더링해 유리 효과를 낸다. 헤더 메뉴 호버로 드롭다운이 열리면 헤더의 실제 높이/내용이 바뀌므로, liquidGL이 스냅샷을 재캡처하거나 렌즈를 재생성할 가능성이 있다. 이 재생성 타이밍에 "타겟은 이미 숨겨졌는데 새 캔버스는 아직 그려지지 않은" 프레임 갭이 생기면 헤더가 순간(또는 웨일에서는 지속적으로) 안 보이게 될 수 있다. 웨일은 Chromium 기반이지만 WebGL 컨텍스트·캔버스 처리, requestAnimationFrame 타이밍이 크롬과 다를 수 있어 이 갭이 유독 웨일에서 크거나 캔버스 렌더 자체가 실패할 가능성이 있다.

## 진단 절차
1. `client/vendor/liquidgl/liquidGL.js`에서 타겟 요소의 opacity/visibility를 조작하는 코드 지점을 전부 찾아라(생성자·resize 핸들러·리캡처 함수 등). 어떤 이벤트(resize, mutation, scroll)가 재캡처를 트리거하는지 특정하라.
2. `useLiquidGlass.js` 훅에서 드롭다운이 열릴 때(헤더 높이 변화) liquidGL 재초기화나 refresh를 호출하는 로직이 있는지 확인하라. MutationObserver나 ResizeObserver가 헤더에 걸려있고, 드롭다운 오픈으로 인한 높이 변화가 그 옵저버를 트리거하는지 확인.
3. 캔버스 렌더링이 실패하거나 지연될 때 타겟이 자동으로 다시 보이는 폴백(fallback)이 있는지 확인하라. 없다면 이것이 근본 원인이다.
4. 웨일 브라우저의 User-Agent 문자열, WebGL 컨텍스트 생성 가능 여부, html2canvas 스냅샷 성공 여부를 분기 처리하는 코드가 있는지 확인하라.

## 수정 방향
1. 드롭다운 오픈으로 인한 헤더 레이아웃 변화가 liquidGL 재캡처를 트리거하지 않게 하라. 헤더 자체(고정 높이 바)와 드롭다운 패널(absolute, 헤더 높이에 영향 없음)을 분리해, 드롭다운이 열려도 liquidGL이 감시하는 요소의 크기가 변하지 않게 하는 것이 근본 해결이다.
2. 캔버스 렌더가 아직 준비 안 됐거나 실패한 프레임에서는 타겟 요소가 항상 보이는 상태(opacity 유지)를 기본값으로 하고, 캔버스가 성공적으로 그려진 다음에만 타겟을 투명화하는 순서로 바꿔라(현재 순서가 반대라면 이것이 원인이다).
3. WebGL 컨텍스트 생성 실패나 html2canvas 실패 시 명시적으로 liquidGL을 비활성화하고 일반 CSS 글래스(box-shadow, backdrop-filter)로 폴백하는 안전장치를 추가하라. 이미 32/33번에서 이 폴백 원칙이 있었으면 그것이 웨일에서 실제로 작동하는지 확인하고, 안 되면 고쳐라.
4. 헤더는 사이트의 핵심 내비게이션이므로, liquidGL 적용으로 인한 어떤 실패 모드에서도 헤더 자체가 완전히 사라지는 일은 없어야 한다는 것을 최우선 불변식으로 삼아라. 필요하면 헤더에서는 liquidGL 사용을 재검토하고, 안전한 CSS 글래스만 쓰는 것도 옵션으로 고려하라(전시회 CTA 등 비핵심 요소에만 liquidGL 유지).

## 완료 조건
1. 재현 가능한 원인을 코드에서 특정(가능하면 실제 재현: Whale User-Agent 에뮬레이션 또는 유사 조건으로).
2. 헤더가 어떤 상황에서도 완전히 사라지지 않도록 안전장치를 넣어라(최소한 이 불변식은 반드시 만족).
3. build 성공, 커밋·푸시·배포.
4. 사용자가 웨일에서 재확인해야 할 정확한 시나리오(어느 메뉴, 몇 초 대기 등)를 명시.
5. PROGRESS.md 기록.