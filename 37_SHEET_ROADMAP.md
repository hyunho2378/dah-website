# 37_SHEET_ROADMAP — 접수 시트·접수폼·로드맵 학기 관리 (단독 → 병렬 3 → 단독)

## 절대 원칙
1. 색상은 CI.md HEX만. 새 색 생성 금지. 사용자 원문 변경 금지.
2. 네이티브 select·date·기본 라디오 노출 금지. 전부 커스텀 컴포넌트.
3. 드래그 구현 시 `matchMedia('(pointer: coarse)')`로 터치 감지해 draggable 속성을 걸지 마라(iOS Safari에서 자식 탭이 삼켜진다 — PITFALLS 참조).
4. 표·시트는 어떤 상호작용에도 레이아웃이 움직이면 안 된다.

---

# STEP 1 — 단독 (공용 선결, 나머지가 의존)

## G1. 시트용 컬럼 헤더 필터 (구글 시트 방식)
표 컬럼 헤더를 클릭하면 그 컬럼의 고유값 목록이 드롭다운으로 뜨고, 체크박스로 원하는 값만 선택해 필터링하는 공용 컴포넌트를 만들어라.
- 헤더에 필터 아이콘, 필터 적용 중이면 활성 표시
- 전체 선택·전체 해제·검색(값 많을 때)
- 정렬(오름/내림)도 같은 드롭다운에서
- 드롭다운은 표 레이아웃에 영향을 주지 않게 absolute·portal로 띄운다
- 네이티브 select 금지

## G2. 논-시프트 토스트
"셀을 복사했습니다" 같은 알림이 표를 밀어내지 않도록, 토스트를 `position: fixed` 오버레이로 띄우는 공용 컴포넌트를 만들어라. 문서 흐름에 공간을 차지하지 않아야 한다(레이아웃 시프트 0). 화면 하단 또는 우하단 고정, 자동 소멸, z-index는 tokens 위계 최상단.

## G3. 커스텀 세그먼트 전환 컨트롤
"1학기 / 2학기"처럼 하나만 노출하고 눌러서 다른 값으로 바꾸는 컨트롤을 공용으로 만들어라. 두 가지 형태 지원: (a) 세그먼트(둘 다 보이고 하나 활성) (b) 단일 노출 후 클릭 시 목록에서 선택. 접수폼에서는 (b), 어드민에서는 (a)를 쓴다. 네이티브 select 금지.

## G4. 밝은 문서 표면(Reading Surface) 테마
접수 현황 시트에서 쓰는 밝은 표면 스타일을 공용 "문서 표면" 스타일로 정리하라. 긴 글을 읽는 화면(공지 상세, 시트)에 쓰는 예외 표면이다.
- 배경은 밝은 중성색, 본문 텍스트는 어두운 색으로 대비 확보(4.5:1 이상)
- 링크·강조는 CI 보라 계열 유지(밝은 배경 대비 충분한 명도 — Deep #6844C4 계열 사용, 연보라 금지)
- 사이트 전역 다크 테마는 유지하고, 이 표면만 예외로 명시적으로 적용
- COMPONENTS.md에 "다크 사이트 내 밝은 읽기 표면" 규칙으로 문서화

STEP 1 빌드 확인 후 STEP 2 병렬.

---

# STEP 2 — 병렬 3

소유: H1=접수 시트 페이지. H2=접수폼·접수 안내·상담. H3=교과목 대시보드·로드맵·공지 상세.
공용(G1~G4)은 수정 금지, 사용만.

## AGENT-H1 — 접수 현황 시트

### H1-1. 마진 이탈 수정 + 행 확장
시트 콘텐츠가 좌우 마진 밖으로 벗어난다. 표를 Container 안에 두고, 넘치는 부분은 페이지가 아니라 **표 래퍼 내부 가로 스크롤**로 격리하라(페이지 가로 스크롤 0).
내용이 긴 셀은 기본적으로 1줄로 말줄임하고, **행을 클릭하면 그 행이 확장되어 전체 내용을 보여주게** 하라(아코디언). 확장 시에도 컬럼 폭·표 위치는 변하지 않아야 한다.

### H1-2. 자동 새로고침 UI 정리
자동 새로고침 "꺼짐" 토글 버튼을 제거하라. 자동 새로고침은 항상 켜진 상태로 두고, 수동 새로고침 버튼과 마지막 갱신 시각 표시만 남긴다.

### H1-3. 컬럼 헤더 필터 적용
과목별 보기를 별도 탭이 아니라 **컬럼 헤더 필터(G1)**로 처리하라. 과목 컬럼 헤더를 눌러 원하는 과목만 선택해 보는 방식. 다른 컬럼(참가 유형, 학기 등)도 동일하게 필터 가능하게.

### H1-4. 컬럼 정리
- 이미지 컬럼 삭제(접수에서 이미지를 받지 않는다)
- 컬럼 순서에서 **작품명이 작품 설명보다 왼쪽**에 오게 재배치

### H1-5. 복사 토스트
셀·행 복사 시 알림을 G2 오버레이 토스트로 교체하라. 복사해도 표가 1px도 움직이지 않아야 한다. 구글 시트와 동일하게 선택 셀에 테두리 강조만 남긴다.

### H1-6. 하단 탭 — 접수자 정보 시트 [신설]
구글 시트처럼 화면 하단에 시트 탭을 만들어라.
- 탭 1: 접수 현황(기존 전체 데이터, 컬럼 삭제 외 변경 금지)
- 탭 2: 접수자 정보 — 접수한 사람의 인적사항만(이름·소속·학번·이메일·연락처·참가 유형·접수일). 작품 정보는 제외.
기존 데이터를 지우지 말고 뷰만 분리한다. 두 탭 모두 필터·복사·CSV·엑셀 내려받기 지원.

### H1-7. 비밀번호 초기화 [권한 필요]
접수자 정보 탭에서 admin 이상 권한자가 "비밀번호 초기화" 버튼을 누르면 해당 접수의 수정용 비밀번호가 `1234`로 재설정되게 하라.
- 서버에서 권한 검증(admin·owner만), 클라이언트 권한만 믿지 마라
- 실행 전 확인 모달("○○○님의 접수 비밀번호를 1234로 초기화합니다")
- 비밀번호는 해시로 저장되어야 하며 초기화도 해시로 저장
- 초기화 이력(누가 언제)을 기록

## AGENT-H2 — 접수폼·접수 안내·상담

### H2-1. 접수 안내 상단 정리
"접수 안내 / 제18회 … 프로젝트 전시회 / 접수 대상 …" 블록의 상단 여백이 과하고 텍스트가 너무 크다. 상단 여백을 줄이고 타이틀 스케일을 한 단계 낮춰, 첫 화면에서 안내 내용이 충분히 보이게 하라. 스크롤 없이 핵심 안내와 시작 버튼이 보이는 것을 목표로.

### H2-2. 회차 지정 가능하게
접수폼·접수 안내에 표시되는 "제18회"를 하드코딩하지 말고, 어드민 전시회 설정에서 지정한 회차(ordinal)를 읽어 표시하라. 회차 입력 UI가 없으면 추가하고, full_title("제N회 디지털인문예술전공 프로젝트 전시회") 자동 조합 규칙을 따른다.

### H2-3. 학기 선택 방식 변경
접수폼의 과목 선택에서 1학기·2학기 과목을 동시에 전부 나열하지 마라. **G3의 단일 노출형 컨트롤**로 현재 학기 하나만 보여주고, 눌러서 다른 학기로 전환하면 그 학기 과목만 나타나게 하라. 기본값은 어드민이 지정한 현재 학기. 네이티브 select 금지, 과목 자체는 카드형 라디오 유지.

### H2-4. 어드민 학기 지정
전시회 설정(접수 관련)에서 **현재 접수 대상 학기(1학기/2학기)를 선택**할 수 있게 하라. 선택된 학기의 과목만 접수폼 기본값으로 노출된다.

### H2-5. 상담 신청 보강
- 이메일 입력 칸을 **필수**로 추가하라(기존 "연락처(전화 또는 이메일)"와 별개로 이메일 단독 필수 필드). @ 형식 검증.
- 개인정보 수집 항목 안내에 이메일을 반영하라.
- 상담 신청 페이지 전체를 **영문 지원**하게 하라(라벨·안내·동의 문구·버튼·에러 메시지 전부). 해외 지원자 대응. 영문 번역은 대학 학과 수준의 격식으로.

## AGENT-H3 — 교과목 대시보드·로드맵·공지 상세

### H3-1. 교과목 대시보드 레이아웃 재설계
현재 과목 이름과 편집·삭제 버튼 사이가 우측 마진 끝까지 벌어져 공간이 낭비된다.
- 좌측 절반: 전체 과목 목록. 과목명 바로 옆에 편집·삭제 아이콘을 붙여 간격을 최소화.
- 우측 절반: **학기 박스**(1학기 / 2학기). 해당 학기에 개설되는 과목을 담는다.

### H3-2. 드래그 배치
좌측 과목을 우측 학기 박스로 드래그해 넣을 수 있게 하라.
- **좌측 원본은 사라지지 않는다**(복사 개념). 드래그하는 동안에만 잠시 반투명 처리하고, 드롭 후 원래대로 복귀.
- 같은 학기에 **중복 추가 금지**(이미 있으면 드롭 무시 + 안내).
- 학기 박스 안에서 제거(x) 가능.
- 터치 기기에서는 draggable을 걸지 말고(iOS Safari 이슈), 대신 과목 항목의 "추가" 버튼으로 같은 동작을 제공하라.

### H3-3. 학기 전환·추가
우측 학기 박스 상단에서 대상 학기를 전환할 수 있게 하라(예: 2026-2학기 기준으로 과거·미래 이동). **학기를 새로 추가**할 수도 있어야 한다(연도·학기 입력). G3 컨트롤 사용, 네이티브 select 금지.
데이터 모델: `semester_offerings` 같은 테이블로 (연도, 학기, 과목id) 조합을 저장. 기존 curriculum 데이터는 건드리지 마라.

### H3-4. 공개 로드맵 연도별 보기 + 현재 학기 강조
교육과정 페이지 하단 로드맵에서:
- 연도·학기를 선택해 그 학기에 실제 개설된 과목을 볼 수 있게 하라(H3-3에서 저장한 데이터 사용).
- **현재 학기(어드민이 지정한 학기)에 개설된 과목에는 프라이머리 은은한 글로우**를 적용하라(36번에서 만든 글로우 스타일 재사용, 상시 발광은 절제하고 눈에 띄되 과하지 않게).
- 개설 정보가 없는 학기는 "개설 정보 없음"으로 표시하고 빈 화면을 만들지 마라.

### H3-5. 공지 상세 밝은 표면 적용
공지사항 상세 페이지를 G4의 밝은 읽기 표면으로 전환하라. 긴 글 가독성이 목적이다.
- 본문 영역만 밝은 표면, 헤더·푸터 등 사이트 크롬은 다크 유지
- 본문 텍스트 대비 4.5:1 이상, 링크·강조는 밝은 배경에서 충분히 어두운 보라(#6844C4 계열)
- 첨부·이미지 갤러리·메타 정보도 밝은 표면에 맞게 조정
- 자료실 상세도 동일 패턴을 쓸지 검토하고, 적용하면 일관되게

---

# STEP 3 — 단독 통합
1. 공용 컴포넌트 충돌 확인, 네이티브 select·date 잔존 0건 재검색.
2. 표·시트가 필터·복사·행확장 어느 동작에서도 레이아웃 시프트 0인지 확인.
3. 320~3840 전 구간에서 시트·접수폼·로드맵 가로 스크롤 0(표 내부 스크롤은 허용).
4. build, 커밋·푸시·배포.
5. 배포 확인: 시트 마진·필터·탭·비밀번호 초기화, 접수폼 학기 전환·회차, 상담 이메일·영문, 로드맵 연도별·글로우, 공지 상세 밝은 표면.
6. PROGRESS.md 기록.