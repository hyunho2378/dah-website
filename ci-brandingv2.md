# DAH 로고 색상 사용 규정  
## 단색 확정 버전 · Monochrome Logo Color Guidelines

---

## 0. 기본 원칙

DAH 로고는 D·A·H 세 조각이 **항상 동일한 하나의 색상**으로 표현되는 단색 로고를 원칙으로 한다.

로고의 세 조각은 서로 다른 색상으로 분리하지 않는다. 색상 차이를 통해 트랙이나 조형 요소를 설명하는 방식은 공식 로고가 아닌 **브랜드 그래픽 모티브**에서만 허용한다.

### 공식 로고의 정의

```text
공식 로고 = 동일한 색상의 D·A·H 세 조각 + 승인된 시그니처 조합
```

### 공식 로고에 허용되는 색상

| 버전 | HEX | 기본 용도 |
|---|---|---|
| Light Logo | `#F7F5FC` | 어두운 배경 |
| Purple Logo | `#815FD7` | 브랜드 강조, 중·저명도 배경 |
| Dark Logo | `#211A31` | 밝은 배경, 인쇄 |
| Deep Dark Logo | `#100D18` | 매우 밝은 배경, 고대비 인쇄 |

---

# 1. 로고 색상 버전

## 1.1 Light Logo

```text
HEX: #F7F5FC
명칭: DAH Light Logo
```

어두운 배경 위에서 사용하는 기본 반전 로고다.  
완전한 순백색보다 부드러운 보랏빛 화이트를 사용하여 DAH의 전체 색상 체계와 조화를 이룬다.

### 권장 배경

- `#100D18` Deep Purple Black
- `#171321` Purple Black 2
- `#211A31` Glass Surface
- 어두운 사진 및 영상
- 어두운 보라색 그래픽 면

---

## 1.2 Purple Logo

```text
HEX: #815FD7
명칭: DAH Purple Logo
```

DAH의 대표 보라색을 사용하는 기본 강조 로고다.

### 권장 배경

- 흰색 또는 밝은 회색
- 중성색 카드
- 저명도 유리 패널
- 브랜드 그래픽의 밝은 영역
- 밝은 문서 및 인쇄물

Purple Logo는 브랜드를 직접적으로 드러내는 버전이지만, 모든 배경에서 사용할 수 있는 만능 로고는 아니다. 배경과의 명도 대비가 충분할 때만 사용한다.

---

## 1.3 Dark Logo

```text
HEX: #211A31
명칭: DAH Dark Logo
```

밝은 배경 위에서 사용하는 기본 어두운 로고다.

### 권장 배경

- `#FFFFFF`
- `#F7F5FC`
- `#F2F0F6`
- 밝은 회색 문서
- 밝은 인쇄용지
- 밝은 사진의 비어 있는 영역

---

## 1.4 Deep Dark Logo

```text
HEX: #100D18
명칭: DAH Deep Dark Logo
```

가장 높은 명도 대비가 필요한 밝은 배경에서 사용한다.

### 권장 배경

- 흰색 배경
- 매우 밝은 회색
- 인쇄물의 넓은 여백
- 공식 문서
- 흑백 출력 환경

Dark Logo와 Deep Dark Logo 중에서는 배경과의 대비가 더 높은 쪽을 선택한다.

---

# 2. 배경별 로고 사용 규정

## 2.1 딥 퍼플 블랙 배경

### 대상 배경

| 배경 | HEX |
|---|---|
| 메인 배경 | `#100D18` |
| 섹션 배경 | `#171321` |
| 어두운 보조 배경 | `#211A31` |
| Deep Purple 계열 | `#4B2D99` 이하의 어두운 보라 |

### 권장 로고

| 우선순위 | 로고 |
|---|---|
| 1순위 | Light Logo `#F7F5FC` |
| 2순위 | Purple Logo `#815FD7` |
| 사용 제한 | Dark Logo `#211A31` |

### 사용 원칙

- 공식 헤더, 시그니처, 파비콘에는 **Light Logo**를 우선 사용한다.
- 대형 히어로 영역이나 브랜드 강조 영역에는 **Purple Logo**를 사용할 수 있다.
- `#211A31` 로고는 `#100D18` 배경에서 명도 차이가 부족하므로 사용하지 않는다.
- 로고 투명도를 임의로 낮추지 않는다.
- Light Logo의 불투명도는 원칙적으로 100%를 유지한다.

### 최소 명도 대비

- 본문 텍스트처럼 식별되어야 하는 공식 로고는 최소 **4.5:1 수준의 대비**를 목표로 한다.
- 대형 로고 및 장식적 심벌은 최소 **3:1 이상**을 확보한다.
- `#F7F5FC` 로고는 `#100D18`, `#171321` 위에서 기본 사용이 가능하다.
- `#815FD7` 로고는 어두운 배경에서도 충분한 대비가 확보되는지 실제 배치별로 확인한다.

### 주의사항

```text
#100D18 배경 + #211A31 로고 = 금지
#171321 배경 + #211A31 로고 = 금지
```

두 색은 모두 어두운 보라 계열이므로 로고의 외곽과 조각 구조가 약해진다.

---

## 2.2 유리 패널 배경

### 대상 배경

- 반투명 `#211A31` 패널
- `#171321` 기반 글래스 카드
- 배경이 비치는 반투명 UI 표면
- 보라색 블러 또는 글로우가 포함된 패널

### 권장 로고

| 패널 상태 | 권장 로고 |
|---|---|
| 어두운 글래스 패널 | Light Logo `#F7F5FC` |
| 중간 명도의 글래스 패널 | Light Logo 또는 Purple Logo |
| 밝은 글래스 패널 | Dark Logo `#211A31` |
| 배경 이미지가 비치는 패널 | 단색 보호 영역 위 Light Logo |

### 사용 원칙

유리 패널은 배경이 고정되지 않고 내부에 이미지·블러·빛의 변화가 존재하므로, 패널의 평균 색상이 아니라 **로고가 실제로 놓이는 영역의 배경**을 기준으로 판단한다.

- 어두운 패널에서는 Light Logo를 사용한다.
- 밝은 패널에서는 Dark Logo를 사용한다.
- 패널 내부의 배경이 복잡하거나 밝기가 변하면 로고 뒤에 보호 영역을 둔다.
- 로고를 글래스 효과와 동일하게 반투명 처리하지 않는다.
- 로고에는 별도의 블러, 배경 흐림, 유리 질감을 적용하지 않는다.

### 권장 보호 영역

로고 뒤에 다음과 같은 단색 또는 반투명 영역을 둘 수 있다.

```text
어두운 패널: #100D18 / 85–95%
밝은 패널: #F7F5FC / 85–95%
```

보호 영역은 로고를 장식하기 위한 것이 아니라 식별성을 확보하기 위한 장치다.

### 최소 명도 대비

유리 패널은 배경 변화가 발생하므로 정적 화면 기준으로만 판단하지 않는다.

- 로고가 놓이는 전체 영역에서 최소 **3:1 이상의 대비**를 확보한다.
- 네비게이션, 버튼, 공식 시그니처처럼 정보 전달 기능이 있는 경우 **4.5:1 이상**을 권장한다.
- 패널의 투명도를 높여 로고와 배경의 구분이 약해질 경우 보호 영역을 적용한다.

---

## 2.3 밝은 회색·흰색 배경

### 대상 배경

- `#FFFFFF`
- `#F7F5FC`
- `#F2F0F6`
- 밝은 회색 인쇄지
- 공식 문서의 흰색 여백

### 권장 로고

| 배경 명도 | 권장 로고 |
|---|---|
| 흰색 | Deep Dark Logo `#100D18` |
| 매우 밝은 회색 | Dark Logo `#211A31` |
| 브랜드 강조 영역 | Purple Logo `#815FD7` |

### 사용 원칙

- 공식 문서, 계약서, 리포트, 명함에는 Dark Logo 또는 Deep Dark Logo를 우선 사용한다.
- 브랜드 포스터나 홍보물에서 보라색을 강조할 때는 Purple Logo를 사용한다.
- 흰색 배경 위에 Light Logo를 사용하지 않는다.
- `#C8B9F2` 또는 `#A286E9`를 공식 로고 색상으로 사용하지 않는다.
- 밝은 회색 배경에서 `#815FD7`의 대비가 충분하지 않으면 Dark Logo로 전환한다.

### 최소 명도 대비

- 공식 문서와 본문형 시그니처: 최소 **4.5:1 이상**
- 대형 심벌: 최소 **3:1 이상**
- 작은 파비콘이나 작은 워드마크는 명도 대비보다 형태 식별성이 우선되므로 Deep Dark Logo를 사용한다.

---

## 2.4 사진·영상 배경

사진과 영상은 배경의 밝기와 복잡도가 계속 바뀌므로, 로고를 사진 위에 직접 얹는 것은 제한한다.

### 권장 로고 선택

| 사진 영역 | 권장 로고 |
|---|---|
| 어두운 단색 영역 | Light Logo |
| 밝은 단색 영역 | Deep Dark Logo |
| 밝기 변화가 큰 영역 | 보호 영역 + Light 또는 Dark Logo |
| 복잡한 인물·사물 영역 | 보호 영역 필수 |

### 사용 원칙

1. 사진 위에서는 우선적으로 **비어 있는 영역**을 찾아 로고를 배치한다.
2. 로고 조각과 유사한 곡선·삼각형·기둥 형태가 배경에 겹치지 않도록 한다.
3. 배경이 복잡한 경우 로고를 직접 배치하지 않고 보호 영역을 사용한다.
4. 로고에 외곽선을 추가하여 사진 위에서 억지로 띄우지 않는다.
5. 사진 자체에 보라색 필터를 적용해 로고와 강제로 맞추지 않는다.
6. 영상에서는 장면 전환 중에도 로고의 명도 대비가 유지되는지 확인한다.

### 권장 보호 영역

#### 어두운 사진

```text
#100D18, 80–90% 불투명도
Light Logo #F7F5FC
```

#### 밝은 사진

```text
#F7F5FC, 85–95% 불투명도
Deep Dark Logo #100D18
```

보호 영역의 형태는 직사각형 또는 부드러운 라운드 패널을 사용할 수 있다. 단, 로고 자체에 라운드 박스를 결합하여 새로운 로고처럼 보이게 해서는 안 된다.

### 최소 명도 대비

- 로고가 정보 전달 목적으로 사용되면 전체 노출 구간에서 **4.5:1 이상**을 확보한다.
- 영상의 특정 프레임에서 대비가 무너지는 경우 배경 보호 영역을 사용한다.
- 배경 변화로 대비가 지속적으로 확보되지 않으면 사진 위 직접 사용을 금지한다.

---

## 2.5 보라 단색 면 위

### 대상 배경

- `#815FD7`
- `#6844C4`
- `#4B2D99`
- `#A286E9`
- `#C8B9F2`

### 권장 로고

| 배경 | 권장 로고 |
|---|---|
| `#815FD7` | Light Logo `#F7F5FC` |
| `#6844C4` | Light Logo `#F7F5FC` |
| `#4B2D99` | Light Logo `#F7F5FC` |
| `#A286E9` | Deep Dark Logo `#100D18` |
| `#C8B9F2` | Deep Dark Logo `#100D18` |

### 사용 원칙

- 배경과 로고를 동일한 보라색으로 맞추지 않는다.
- `#815FD7` 배경 위에 Purple Logo `#815FD7`를 사용하지 않는다.
- `#C8B9F2` 배경 위에는 Light Logo보다 Deep Dark Logo를 우선한다.
- 보라색 면 위에서는 단순히 색상 계열이 아니라 명도 대비를 기준으로 버전을 선택한다.

### 금지 조합

```text
#815FD7 배경 + #815FD7 로고
#6844C4 배경 + #815FD7 로고
#A286E9 배경 + #C8B9F2 로고
```

유사한 색상끼리 배치하면 로고의 세 조각이 배경에 흡수된다.

---

# 3. 색상 사용 규칙

## 3.1 전체 색상 체계

| 색상명 | HEX | 권장 용도 | 사용 제한 |
|---|---|---|---|
| Primary Purple | `#815FD7` | 대표 로고, 버튼, 링크, 강조 | 넓은 본문 배경 |
| Light Purple | `#C8B9F2` | 하이라이트, 그래픽, 밝은 배경 | 공식 로고 |
| Mid Purple | `#A286E9` | 보조 강조, 인터랙션 | 본문 텍스트 |
| Deep Purple | `#6844C4` | 활성 상태, 진한 그래픽 | 넓은 면적 |
| Deep Purple Dark | `#4B2D99` | 강한 강조, 깊은 그래픽 | 본문 배경 남용 |
| Deep Purple Black | `#100D18` | 메인 배경 | 밝은 텍스트 없이 단독 사용 |
| Purple Black 2 | `#171321` | 섹션, 카드 배경 | 로고의 중간 대비 배경 |
| Glass Surface | `#211A31` | 유리 패널, 카드 표면 | 작은 로고와 저대비 조합 |
| Text Primary | `#F7F5FC` | 제목, 핵심 정보 | 밝은 배경 위 단독 사용 |
| Text Secondary | `#C9C3D5` | 본문, 설명 | 작은 크기에서 저대비 사용 |
| Text Tertiary | `#938BA5` | 메타데이터, 보조 정보 | 본문 핵심 문장 |

---

## 3.2 보라색의 면적 비율

DAH는 보라색을 많이 사용하는 브랜드가 아니라, 어두운 공간 안에 보라색이 정확히 등장하는 브랜드로 운영한다.

### 권장 면적 비율

```text
딥 퍼플 블랙·중성 배경: 70–80%
글래스·보조 표면: 10–20%
텍스트·화이트 계열: 5–10%
보라색 강조: 최대 5–10%
```

웹사이트의 주요 화면에서는 `#815FD7` 이상의 강한 보라색 면적을 전체 화면의 **10% 이내**로 제한하는 것을 권장한다.

### 보라색을 넓게 사용할 수 있는 경우

- 캠페인 표지
- 입학 홍보 포스터
- 특정 이벤트 페이지
- 그래픽 모티브가 중심인 배너
- 트랙 소개용 시각물

단, 공식 로고의 단색 원칙은 보라색 면적과 관계없이 항상 유지한다.

---

## 3.3 본문 텍스트에 보라색을 사용하지 않는 이유

보라색은 DAH의 브랜드 식별 색상이지만, 긴 본문을 읽게 하는 기본 텍스트 색상으로는 사용하지 않는다.

### 이유

1. 어두운 배경에서 긴 보라색 문장은 가독성이 빠르게 저하된다.
2. 보라색이 본문 전체에 사용되면 강조와 일반 정보의 위계가 사라진다.
3. 글래스모피즘 환경에서 보라색은 배경의 잔광과 섞일 수 있다.
4. 보라색 본문은 장식적이고 감성적인 인상을 강화하여 학술적 신뢰도를 약화시킬 수 있다.
5. DAH의 보라는 내용 자체보다 **중요도와 인터랙션을 표시하는 색**으로 기능해야 한다.

### 본문 텍스트 규칙

- 제목: `#F7F5FC`
- 본문: `#C9C3D5`
- 보조 설명: `#938BA5`
- 링크·키워드·상태: `#A286E9` 또는 `#C8B9F2`
- 본문 전체에 `#815FD7`를 적용하지 않는다.
- 보라색 문장을 연속해서 세 줄 이상 배치하지 않는다.
- 보라색 텍스트와 회색 텍스트를 같은 위계에서 혼용하지 않는다.

---

## 3.4 강조·링크·버튼·아이콘 위계

### 강조 위계

| 단계 | 요소 | 권장 색상 |
|---|---|---|
| 1단계 | 핵심 브랜드 강조, 주요 CTA | `#815FD7` |
| 2단계 | 링크, 보조 CTA, 인터랙션 | `#A286E9` |
| 3단계 | 선택 상태, 하이라이트 | `#C8B9F2` |
| 4단계 | 활성·pressed 상태 | `#6844C4` 또는 `#4B2D99` |

### 버튼

#### Primary Button

```text
배경: #815FD7
텍스트: #F7F5FC
Hover: #A286E9
Pressed: #6844C4
```

#### Secondary Button

```text
배경: 투명 또는 #211A31
테두리: #815FD7
텍스트: #C8B9F2
Hover: #815FD7
```

#### Ghost Button

```text
배경: 투명
텍스트: #C8B9F2
Hover 배경: #211A31
```

### 링크

- 기본 링크: `#A286E9`
- Hover: `#C8B9F2`
- 방문 또는 활성 상태: `#815FD7`
- 링크 텍스트에 밑줄을 제거할 경우 색상만으로 구분하지 않도록 별도 인터랙션을 제공한다.

### 아이콘

- 기본 아이콘: `#C9C3D5`
- 활성 아이콘: `#A286E9`
- 핵심 기능 아이콘: `#815FD7`
- 비활성 아이콘: `#938BA5`
- 아이콘 전체를 보라색으로 통일하지 않는다.

---

# 4. 그래픽 모티브 사용 규정

세 조각을 서로 다른 색으로 변주하는 방식은 공식 로고가 아니라 **DAH 브랜드 그래픽 모티브**로만 사용한다.

## 4.1 허용 색상

| 조각 모티브 | 허용 색상 |
|---|---|
| D 모티브 | `#C8B9F2` |
| A 모티브 | `#A286E9` |
| H 모티브 | `#815FD7` |

이 조합은 로고가 아니라 다음과 같은 시각 요소로만 사용한다.

- 포스터 배경
- 행사 배너
- 웹 섹션 장식
- 트랙 소개 그래픽
- 발표자료 표지
- SNS 카드뉴스
- 모션 그래픽
- 패턴 및 배경 오브젝트

## 4.2 모티브와 로고의 구분

| 구분 | 공식 로고 | 그래픽 모티브 |
|---|---|---|
| 색상 | 단색만 | 세 조각 색상 변주 허용 |
| 사용 위치 | 헤더, 파비콘, 문서, 시그니처 | 배경, 장식, 캠페인 |
| 조형 변형 | 금지 | 제한적 변형 허용 |
| 색상 순서 | 항상 동일 | 매체에 따라 보조적 변주 가능 |
| 식별 역할 | DAH 공식 식별자 | 브랜드 분위기와 주제 전달 |

그래픽 모티브가 로고처럼 오인될 수 있는 위치에 놓이는 경우에는 반드시 공식 단색 로고를 함께 배치한다.

---

# 5. 금지 사항

## 5.1 로고 색상 관련 금지

| 금지 사례 | 이유 |
|---|---|
| D·A·H 조각마다 서로 다른 색을 적용 | 하나의 로고로 응집되지 않고 세 요소가 분리되어 보임 |
| 그라디언트 적용 | 단색 로고 체계가 무너지고 작은 크기에서 형태가 흐려짐 |
| 네온 효과 적용 | 차분하고 고급스러운 DAH의 인상과 어긋남 |
| 그림자 추가 | 로고 외부에 불필요한 시각 효과가 생김 |
| 외곽선 추가 | 승인되지 않은 이중 색상과 새로운 실루엣이 발생함 |
| 투명도 임의 조정 | 배경에 따라 식별성이 불안정해짐 |
| 색상 필터 또는 블렌딩 모드 적용 | 출력·디지털 환경에 따라 색이 달라짐 |
| 승인되지 않은 보라색 사용 | 브랜드 컬러 시스템의 일관성이 깨짐 |
| `#C8B9F2`, `#A286E9`를 공식 로고에 사용 | 그래픽 모티브와 공식 로고가 혼동됨 |

---

## 5.2 배경과 로고 조합 금지

| 금지 조합 | 이유 |
|---|---|
| `#100D18` 배경 + `#211A31` 로고 | 명도 대비가 낮아 로고 구조가 약해짐 |
| `#171321` 배경 + `#211A31` 로고 | 유리 패널과 로고가 시각적으로 합쳐짐 |
| `#815FD7` 배경 + `#815FD7` 로고 | 색상이 동일하여 로고가 사라짐 |
| `#815FD7` 배경 + `#A286E9` 로고 | 유사한 보라색으로 조형 경계가 약함 |
| `#C8B9F2` 배경 + `#F7F5FC` 로고 | 밝은 색끼리의 대비가 부족함 |
| 사진의 복잡한 영역 위 직접 배치 | 배경 요소와 로고가 충돌함 |
| 보라색 글로우 위 Purple Logo | 주변 빛과 로고가 혼합됨 |
| 반투명 배경 위 반투명 로고 | 이중 투명도로 식별성이 저하됨 |
| 로고 위에 텍스처·노이즈 적용 | 공식 식별자의 형태가 불안정해짐 |

---

## 5.3 색상 시스템 관련 금지

| 금지 사례 | 이유 |
|---|---|
| 순수 검정 `#000000` 사용 | DAH의 딥 퍼플 블랙 세계관과 분리됨 |
| 청록·옐로우·핑크 등 대비색 추가 | 보라 단일 계열의 통일성이 깨짐 |
| 임의의 회색 배경 추가 | 지정된 명도 단계와 화면 깊이가 흔들림 |
| 보라색으로 긴 본문 작성 | 읽기 피로와 정보 위계 저하 |
| 모든 버튼을 같은 보라로 처리 | CTA의 우선순위가 구분되지 않음 |
| 보라색 면을 화면 대부분에 사용 | 절제된 고급감이 사라지고 장식성이 과해짐 |
| 텍스트에 `#C8B9F2`를 과도하게 사용 | 밝은 배경과 어두운 배경 모두에서 위계가 불안정함 |
| `#938BA5`를 핵심 본문에 사용 | 명도 대비가 낮아 정보 전달력이 떨어짐 |
| 배경·텍스트·버튼을 모두 보라색으로 처리 | 배경과 기능 요소의 역할이 구분되지 않음 |

---

# 6. 최종 승인 규칙

다음 다섯 가지를 모두 충족할 때만 로고를 승인한다.

```text
1. D·A·H 세 조각이 동일한 하나의 색상인가?
2. 승인된 로고 색상 중 하나를 사용했는가?
3. 배경과 최소 3:1 이상의 명도 대비를 확보했는가?
4. 정보 전달형 로고라면 4.5:1 이상의 대비를 확보했는가?
5. 그라디언트·그림자·외곽선·다색 요소가 없는가?
```

## 최종 의사결정 순서

```text
배경 확인
   ↓
밝기와 복잡도 확인
   ↓
Light / Purple / Dark 버전 선택
   ↓
명도 대비 확인
   ↓
필요 시 보호 영역 적용
   ↓
공식 로고 사용 승인
```

---

# 7. 한 줄 규정

> **DAH 공식 로고는 언제나 하나의 색으로만 사용하며, 배경과의 명확한 명도 대비를 확보한다. 세 조각의 색상 분리는 로고가 아닌 브랜드 그래픽 모티브에서만 허용한다.**