# 34_HEADER_AUDIT — 헤더 회귀 버그 근본 원인 진단 (단독)

## 배경
DragHandle의 iOS Safari draggable 버그를 방금 근본 원인까지 특정해 수정했다(PHASE 21, db87d61). 그와 별개로, 최근 대규모 업데이트(리브랜딩 32_REBRAND 등) 이후 헤더에서 "오류가 나거나 클릭이 안 되거나 사라지거나 안 보이는" 증상이 여러 곳에서 보고된다. 추측하지 말고 실제 원인을 코드·런타임에서 특정하라.

## 진단 절차 (순서대로)

### 1. 최근 변경 이력에서 헤더 관련 파일 특정
git log로 최근 커밋(특히 32_REBRAND, 리브랜딩 이후 커밋들)에서 헤더(Header.jsx, 헤더 드롭다운, GlassDock/모바일 내비, LangSwap, LangContext 등) 관련 파일이 어떤 순서로 어떻게 바뀌었는지 diff를 확인하라.

### 2. 콘솔 에러 실측
로컬(또는 프리뷰) 빌드를 띄우고 헤더가 렌더되는 모든 상태(데스크탑 초기 로드, 각 메뉴 호버, 모바일 뷰포트, 로그인 상태/비로그인 상태, 언어 전환 KR/EN)에서 브라우저 콘솔에 에러가 발생하는지 하나씩 확인해라. 에러 스택트레이스를 그대로 수집해라.

### 3. "사라짐/안 보임" 케이스 재현
다음 각 시나리오에서 헤더가 실제로 안 보이거나 빈 화면이 되는지 재현하라:
- 페이지 최초 로드 직후(로그인 상태 판별 전)
- 언어 전환 직후(KR↔EN)
- 로그인/로그아웃 직후
- 리사이즈(데스크탑↔모바일 브레이크포인트 경계)
- 스크롤 중(헤더가 fixed/sticky일 때)
각 케이스에서 헤더 컴포넌트가 언마운트되는지, 조건부 렌더 분기에서 null을 반환하는 경로가 있는지, 에러 바운더리에 걸려 전체가 사라지는지 확인해라.

### 4. "클릭 안 됨" 케이스 재현
헤더의 각 인터랙티브 요소(메뉴 항목, 드롭다운 하위 항목, KR/EN 토글, 설정 아이콘, 햄버거)를 클릭해 실제로 안 눌리는 게 있는지 확인하고, 안 눌리면 원인을 특정하라(예: z-index로 다른 투명 요소가 위에 덮여있는지, pointer-events:none이 잘못 걸렸는지, 이벤트 핸들러가 리렌더 중 유실되는지, DragHandle과 유사한 draggable류 다른 버그가 있는지).

### 5. 회귀 여부 판단
위에서 발견한 각 증상이 리브랜딩(32번) 커밋 이전에도 있었는지, 그 커밋 이후 생겼는지 git blame/checkout으로 비교해 명확히 하라. 리브랜딩 관련이면 어느 STEP(토큰 교체/W1/W2/W3)의 어떤 변경이 원인인지 특정하라.

## 원칙
- 추측성 "아마 이것 때문일 것"으로 여러 곳을 동시에 고치지 마라. 각 증상마다 실제로 재현하고, 재현된 것만 원인을 특정해 최소 범위로 고쳐라.
- DragHandle 수정처럼 surgical하게: 변경 파일 최소화, 각 수정이 어떤 증상을 없앴는지 1:1로 검증하라.
- 콘솔 에러가 재현되면 그 스택트레이스를 보고서에 그대로 포함해라.
- 색상·레이아웃 리브랜딩 자체를 되돌리지 말고, 그로 인해 깨진 로직(조건부 렌더, 이벤트 핸들러, 상태 관리)만 고쳐라.

## 완료 조건
1. 재현된 모든 헤더 증상 목록 + 각각의 근본 원인 + 수정 내역을 보고서로 작성.
2. npm run build 성공.
3. 커밋·푸시·배포.
4. PROGRESS.md에 기록.
5. 사용자가 실기기에서 재확인해야 할 시나리오를 명시.